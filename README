Submit to the cluster using the following command, give a specific run name for the job with run_name
`source submit.sh run_name`

 you should have snakemake executable from your path, so do so you can edit the .bash_profile, in my case I've installed snakemake
 directly in my home drive
 
 This pipeline is still a bit buggy, while each individual rule is running fine, the overall rna_seq_snakemake file is still not running quite correctly. 
